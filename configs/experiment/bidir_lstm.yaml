# @package _global_

defaults:
  - override /model: deep_res_bidir_lstm
  - override /trainer: mps
  - override /partitioning: default

trainer:
  gradient_clip_val: 15.0 # As described in https://arxiv.org/pdf/1708.08989
  gradient_clip_algorithm: norm

  max_epochs: 30