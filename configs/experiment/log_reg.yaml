# @package _global_

defaults:
  - override /model: log_reg_pl
  - override /trainer: mps
  - override /partitioning: default

partitioning:
  k_folds: null
  stratify: False

trainer:
  max_epochs: 30